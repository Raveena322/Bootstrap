Experiment 4 — Bootstrap (bootstrap4)

Purpose
- Theming and customization experiment: CSS variables, overrides, and custom utilities.

Quick Start
- Install deps: `npm install`
- Start: `npm run dev`
- Build: `npm run build`

Structure
- Contains theming examples and any custom CSS utilities under `src/`.

Notes
- Use this folder to prototype custom themes and CSS variable overrides safely.
