Experiment 2 — Bootstrap (bootstrap2)

Purpose
- Second iteration focusing on responsive utilities and component variants.

Quick Start
- Install dependencies: `npm install`
- Run dev server: `npm run dev`
- Build: `npm run build`

Structure
- `index.html`, `src/main.jsx`, `src/App.jsx` — similar layout to experiment 1.

Notes
- Use this folder to compare breakpoints, responsive helpers, and component behavior.
